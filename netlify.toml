[build]
publish = "dist"                   # ← use "build" if Create React App
functions = "netlify/functions"
command = "npm run build"

[[redirects]]
from = "/api/*"
to = "/.netlify/functions/api/:splat"
status = 200
