[build]
  publish = "dist"         # ⬅️ use "build" for Create React App, "dist" for Vite
  functions = "netlify/functions"
  command = "npm run build"

# Redirect frontend API requests to your Express handler
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200

